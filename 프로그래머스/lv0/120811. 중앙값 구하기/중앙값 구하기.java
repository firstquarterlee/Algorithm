import java.util.Arrays;

class Solution {
    public int solution(int[] array) {
        Arrays.sort(array);
        return array[array.length/2];
    }
}

// Arrays.sort()을 이용하여 배열(Array)을 정렬(sorting)할 수 있습니다. ㅇㅋ 이거로 가보자.